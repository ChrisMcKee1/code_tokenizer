[pytest]
markers =
    unit: Unit tests
    integration: Integration tests
    smoke: Smoke tests
    performance: Performance tests

addopts = 
    --strict-markers
    --cov=code_tokenizer
    --cov-report=xml
    --cov-report=term-missing
    --cov-branch 